---
alwaysApply: true
---

This project runs entirely with Docker Compose.

Prefer Docker commands whenever possible.

Examples:
- docker compose exec frontend npm run lint
- docker compose exec backend python -m pytest

Do not assume Python, Node.js, npm or other dependencies are installed on the host machine.
